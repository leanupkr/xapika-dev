# Xapika 듀얼 도메인 + 이메일 전환 작업 보고

**작업일:** 2026-06-18
**대상:** xapika-web (Xapika Engineering 철도정비 마케팅 사이트)
**한 줄 요약:** `xapika.pl` 와 `xapika.co.kr` 를 **각각 독립적으로 작동·랭킹하는 두 홈페이지**로 만들고, **이메일 발신을 @xapika.pl 로 전환**하면서 문의 메일에 **유입 도메인(.pl/.co.kr)을 표시**하도록 구현. 양쪽 실제 발송·수신 테스트까지 통과.

---

## 1. 목표

| 요청 | 결과 |
|------|------|
| 두 도메인이 같은 홈페이지로 **둘 다 정상 작동** | ✅ 라이브 |
| **SEO를 각각 최적화** (둘 다 검색에서 살아있게) | ✅ self-canonical + 상호 hreflang |
| **이메일 발신을 @xapika.pl 로** | ✅ noreply@xapika.pl |
| 문의 메일에 **.pl / .co.kr 유입 출처 표시** | ✅ SOURCE 필드 |

---

## 2. 웹사이트 — 듀얼 도메인 SEO 아키텍처

한 개의 Vercel 배포가 두 도메인을 모두 서빙하되, **요청 Host 기준으로 런타임 분기**한다.

- **각 도메인 self-canonical**: xapika.pl 페이지는 자기 자신을, co.kr 페이지도 자기 자신을 canonical로 선언 → 중복 콘텐츠로 한쪽이 탈락하지 않음.
- **상호 hreflang 클러스터** (모든 페이지 동일):
  - `en-PL` → `https://xapika.pl/...`
  - `en-KR` → `https://xapika.co.kr/...`
  - `x-default` → `https://xapika.pl/...` (대표 = 폴란드 HQ)
- **sitemap·robots도 호스트별로 분기** (`Vary: Host`): 각 도메인이 자기 URL만 `<loc>`/`Sitemap:` 으로 노출 → canonical 신호 충돌 없음.
- `<html lang>` `ko → en`, OG locale `ko_KR → en_US` 로 교정 (사이트 콘텐츠가 영어이므로).

**효과:** 두 ccTLD가 각자 지역(.pl=유럽/폴란드, .co.kr=한국)에서 **중복 페널티 없이 독립적으로 색인·랭킹**될 수 있음.

### 라이브 검증 (curl)
| 항목 | xapika.pl | xapika.co.kr |
|------|-----------|--------------|
| canonical | `https://xapika.pl/...` (self) | `https://xapika.co.kr/...` (self) |
| hreflang | en-PL/.pl · en-KR/.co.kr · x-default/.pl | 동일 |
| og:url | xapika.pl | xapika.co.kr |
| lang / locale | en / en_US | en / en_US |
| robots Sitemap | xapika.pl/sitemap.xml | xapika.co.kr/sitemap.xml |
| sitemap `<loc>` | xapika.pl URL | xapika.co.kr URL |

---

## 3. 코드 변경 (커밋 `366d805`, main 푸시·배포 완료)

총 34개 파일. 핵심:

| 파일 | 변경 |
|------|------|
| `src/lib/seo-host.ts` *(신규)* | `getRequestOrigin()` (Host 헤더로 origin 판별), `buildHostAlternates()` (hreflang 클러스터), `hostSiteUrl()` |
| `src/lib/seo.ts` | `buildPageMetadata`에 `origin` 인자 추가, BASE_URL 기본값 → `xapika.pl`, OG locale en_US |
| `src/app/layout.tsx` | 정적 metadata → `generateMetadata()` (host-aware), `html lang="en"`, `<SiteJsonLd/>` |
| `src/components/seo/SiteJsonLd.tsx` *(신규)* | Organization/WebSite/Navigation JSON-LD를 host-aware로 (엔티티 `@id`는 .pl로 고정) |
| `src/components/seo/JsonLd.tsx` | 빌더 함수 전부 `origin` 인자 우선 시그니처로 |
| 전 페이지 (22개) | `generateMetadata`에 `await getRequestOrigin()` 주입 |
| `src/app/sitemap.ts` | **동적** sitemap (호스트별 `<loc>` + hreflang, /privacy·/terms 포함) |
| `src/app/robots.ts → robots.txt/route.ts` | 호스트별 `Sitemap:` 라인, `Vary: Host` 헤더 |
| `src/lib/resend.ts` | 발신 기본값 → `noreply@xapika.pl` |
| `src/lib/contactEmail.ts` | **SOURCE(유입 도메인) 표시** 추가 (HTML + 텍스트), 푸터 .pl |
| `src/app/contact/actions.ts` | Host에서 `sourceDomain` 도출 → 메일 데이터에 전달, 이메일 자산은 PL_ORIGIN 고정 |

> 설계 근거: 멀티에이전트 워크플로우(설계 + 적대적 검증)로 작성. 검증에서 잡은 3대 블로커(빌드 깨짐, 정적 sitemap의 canonical 충돌, robots CDN 캐싱)는 모두 반영 후 배포.

---

## 4. DNS

### GoDaddy — `xapika.pl` (계정 "ZP")
| 동작 | 유형 | 이름 | 값 | 비고 |
|------|------|------|----|------|
| **수정** | A | @ | `216.150.1.1` | 옛 휴면서버 185.40.84.48 → Vercel |
| 추가 | TXT | @ | `google-site-verification=...` | GSC 인증 |
| 추가 | TXT | `resend._domainkey` | DKIM 공개키 (218자) | Resend 발신 |
| 추가 | TXT | `send` | `v=spf1 include:amazonses.com ~all` | SPF |
| 추가 | MX | `send` | `feedback-smtp.eu-west-1.amazonses.com` (우선순위 10) | 반송 처리 |
| 추가 | TXT | `_dmarc` | `v=DMARC1; p=none;` | DMARC (모니터링) |
| **유지** | MX | @ | `aspmx.l.google.com` 외 4개 | **Google Workspace 수신 보존** |
| 유지 | NS / CNAME(`_domainconnect`,`www`) / SOA | | | 시스템/기존 |

> ⚠️ **GoDaddy 인라인 폼 버그:** 루트 `TXT @` 추가 시 "레코드 이름 @이(가) 다른 레코드와 충돌합니다" 에러가 뜸 ('도메인 소유권 확인' 헬퍼도 동일). 서브도메인 레코드(resend._domainkey/send/_dmarc)는 정상. GSC 인증은 URL-prefix 방식으로 우회 완료.

### 가비아 — `xapika.co.kr`
변경 없음. 기존 Vercel(A 216.198.79.1) + Resend(Tokyo) 구성 유지. (co.kr Resend 발신은 중단 — 아래 참조)

---

## 5. 이메일 — Resend 전환

- **Resend 무료 플랜 = 도메인 1개 제한** → `xapika.co.kr` 제거하고 `xapika.pl` 추가(교체).
- 신규 도메인: **xapika.pl, 리전 Ireland (eu-west-1), 상태 Verified**. (co.kr은 Tokyo였음. .pl 전용 새 DKIM 키 발급.)
- **수신**은 그대로 Google Workspace(info@xapika.pl) — 루트 MX 보존했으므로 영향 없음.
- **발신** 전환: Vercel 환경변수
  - `CONTACT_FROM_EMAIL` = `Xapika Contact <noreply@xapika.pl>`
  - `NEXT_PUBLIC_SITE_URL` = `https://xapika.pl`
  - → 변경 후 **Redeploy**(366d805 재배포)로 적용. `CONTACT_TO_EMAIL`(info@xapika.pl)·`RESEND_API_KEY`는 그대로.

---

## 6. Google Search Console

- `https://xapika.pl/` **URL-prefix 속성 자동 인증**(Domain name provider/DNS). + **sitemap.xml 제출 완료.**
- 참고: xapika.pl은 옛 폴란드 사이트의 **검색 이력(41클릭·14색인)** 을 이미 보유 → SEO 자산.
- co.kr 속성은 기존 유지(이미 인증·사이트맵 제출됨).

---

## 7. 엔드투엔드 테스트 (실제 발송·수신 확인)

양쪽 도메인의 contact 폼에서 실제 문의를 전송 → info@xapika.pl 수신함에서 직접 확인.

| | xapika.pl 제출 | xapika.co.kr 제출 |
|---|---|---|
| 발신자 | `noreply@xapika.pl` ✅ | `noreply@xapika.pl` ✅ |
| 수신 | info@xapika.pl ✅ | info@xapika.pl ✅ |
| 메일 **SOURCE** | `xapika.pl` ✅ | `xapika.co.kr` ✅ |
| 사무실 | Warsaw — HQ | Seoul |

→ **발신·수신·유입 출처 표시 모두 정상 작동 확인.** (테스트 데이터: Test LeanUp / admin@leanup.kr)

---

## 8. 비용 (Vercel)

- **Pro Plan · Active** (정식 유료, 트라이얼 아님). 월 **$20 고정** + 포함 크레딧 $20.
- 현재 사용량 `$0.08 / $20.00` (0.4%) → 사실상 매달 $20만 청구.
- On-Demand Budget $200 = 크레딧 소진 후 종량제 **소프트 상한**(Pause Off라 알림만, 자동 정지 아님). 저트래픽 사이트라 초과 가능성 거의 없음.

---

## 9. 주의사항 / 남은 것

- ✅ **Vercel Pro 결제 완료** — 사이트 중단 위험 없음. (이전 "트라이얼 만료" 우려는 해소됨.)
- ⏳ **구글 재크롤·색인 반영**: 수일~수주 소요 (정상). 사이트맵 제출했으니 점진 반영됨.
- ℹ️ DMARC는 `p=none`(모니터링)으로 시작. 2~4주 리포트 확인 후 `p=quarantine → reject`로 단계적 강화 권장(선택).
- ℹ️ Resend에서 co.kr 발신은 중단됨(필요 시 재등록 가능하나 무료 플랜은 1개 제한 → 그땐 Pro 업그레이드 필요).

---

*문서 생성: Claude Code · 2026-06-18*
